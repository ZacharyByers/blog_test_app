<h1>Pages</h1>

<% if @pages.any? %>
  <% @pages.each do |page| %>
    <h3>
      <%= link_to page.title, page_path(page) %>
    </h3>
  <% end %>

<% else %>
  <h4> No pages, please create one!</h4>
<% end %>

<%= link_to 'New Blog Post', new_page_path, class: 'btn' %>
